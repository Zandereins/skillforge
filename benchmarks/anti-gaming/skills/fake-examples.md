---
name: fake-examples
description: >
  A skill that repeats identical examples to inflate quality score.
  Use when testing anti-gaming detection for duplicate content.
  Do NOT use for actual code review.
---

# Fake Examples Skill

This skill helps with code review. You should probably use it when you
maybe want to review code. It might help with some things.

## Examples

Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.

## More Examples

Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
Example: Run `review code` to review.
